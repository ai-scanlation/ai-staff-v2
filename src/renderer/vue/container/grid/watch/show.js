// import {
//     style
// } from 'modules';
// import gridStyle from '../src/gridStyle';

module.exports = function show(value) {
    if (typeof value !== 'object') return;
    // for (const key in value) {
    //     // style.set({
    //     //     group: "table",
    //     //     overwrite: true,
    //     //     rules: {
    //     //         "#{$cellSelector}": styling.propertiesToArray(section_, index)
    //     //     }
    //     // }, {
    //     //     cellSelector: styling.cellSelector(this._uid, section, index),
    //     //     lineSelector: styling.lineSelector(this._uid, section, index)
    //     // });
    // }
};
